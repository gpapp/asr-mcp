# Full server base URL, including any path prefix (e.g. behind nginx)
SERVER_URL=http://127.0.0.1:8087/asr-mcp

# API token - generate it on the server: open the web UI and use
# "Generate token" in the Windows Client Token card (or POST /api/token
# while logged in), then paste it here. One token per user.
TOKEN=
